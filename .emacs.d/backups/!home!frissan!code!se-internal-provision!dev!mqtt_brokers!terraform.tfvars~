
terragrunt = {
  include {
    path = "${find_in_parent_folders()}"
  }

  terraform {
    source = "git::ssh://git@bitbucket.org/trelltech/fv-terraform-modules.git//data_mqtt_brokers?ref=HEAD"
  }

  dependencies {
    paths = [
      "../vpc",
      "../../mgmt/bastion_hosts",
    ]
  }
}

environment = "stage"

instance_type = "t2.nano"
availability_zone = "eu-central-1a"
autoscaling_group_min_size = 1
autoscaling_group_max_size = 1
autoscaling_group_desired_capacity = 1
