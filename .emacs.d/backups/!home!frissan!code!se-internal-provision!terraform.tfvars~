
terragrunt = {
  remote_state {
    backend = "s3"
    config {
      region = "eu-central-1"
      bucket = "fortumvarme-terraform-state"
      # ensure every child stores its state in a separate key
      key = "${path_relative_to_include()}/terraform.tfstate"
      encrypt = true
    }
  }
}
