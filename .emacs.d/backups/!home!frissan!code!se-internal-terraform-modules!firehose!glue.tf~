
resource "aws_glue_crawler" "main" {
  name          = "${var.environment}-${var.name}-raw"
  database_name = "${var.environment}-dlb"
  role          = "${aws_iam_role.glue_role.arn}"
  schedule      = "cron(0 0 * * ? *)"

  s3_target {
    path = "s3://${aws_s3_bucket.main.bucket}"
  }
}
