#!/usr/bin/env ansible-playbook
---

- include: 'all.yml'

- name: 'mqtt_pipeline'
  hosts: 'localhost'
  vars:
    mqp_ssh_key_priv: '{{ cicd_ssh_key_priv }}'
  roles:
    - 'supervisor'
    - 'mqtt-pipeline'
